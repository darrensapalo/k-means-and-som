public class ConfigSet{
		int width, height;
		int maxIterations;
		public ConfigSet(int width, int height, int maxIterations) {
			super();
			this.width = width;
			this.height = height;
			this.maxIterations = maxIterations;
		}
	}